---
import type { PageSection } from '../../content/page-schema';
import SectionHead from '../SectionHead.astro';
import Section from './Section.astro';

type Marquee = Extract<PageSection, { type: 'marquee' }>;

interface Props {
  section: Marquee;
}

const { section } = Astro.props;
const speed = section.speed ?? (section.kind === 'wordmarks' ? 26 : 22);
---

{
  section.kind === 'strip' ? (
    // Tamamen dekoratif kayan şerit — referansta aria-hidden, container/section boşluğu yok (tam genişlik).
    <div class="marquee-strip" aria-hidden="true">
      <div class="marquee-strip__track" style={`animation-duration:${speed}s`}>
        <div class="marquee-strip__group">
          {section.items.map((item) => (
            <span>{item} ✦</span>
          ))}
        </div>
        <div class="marquee-strip__group">
          {section.items.map((item) => (
            <span>{item} ✦</span>
          ))}
        </div>
      </div>
    </div>
  ) : (
    <Section anchor={section.anchor} background={section.background} ctas={section.ctas} class="section-partners">
      <SectionHead eyebrow={section.eyebrow} heading={section.heading} lead={section.lead} align="center" />

      <div class="marquee-wordmarks" role="group" tabindex="0">
        <div class="marquee-wordmarks__track" style={`animation-duration:${speed}s`}>
          <ul class="marquee-wordmarks__group">
            {section.items.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
          <ul class="marquee-wordmarks__group" aria-hidden="true">
            {section.items.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </div>
      </div>
    </Section>
  )
}

<style>
  /* --- Lime şerit (hero altı) — referans: 20px yazı, 18px dolgu, 56px aralık --- */
  .marquee-strip {
    overflow: hidden;
    background: var(--accent);
    padding: 18px 0;
  }

  .marquee-strip__track {
    display: flex;
    width: max-content;
    animation-name: marquee;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
  }

  .marquee-strip__group {
    display: flex;
    flex-shrink: 0;
    gap: 56px;
    padding-right: 56px;
  }

  .marquee-strip__group span {
    font: 600 20px var(--font-display);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text-on-light);
    white-space: nowrap;
  }

  /* --- Metin marka isimleri şeridi (partnerler) --- */
  /* Referans: bu bölüm daha sıkışıktır (48-72px dolgu) ve başlığı küçüktür (max 30px). */
  :global(.section-partners) {
    padding: clamp(48px, 6vw, 72px) 0;
  }

  :global(.section-partners .section-head .h2) {
    font-size: clamp(22px, 2.4vw, 30px);
    letter-spacing: -0.01em;
  }

  :global(.section-partners .section-head .lead) {
    font-size: 15px;
    max-width: 640px;
    margin-inline: auto;
  }

  :global(.section-partners .section-head) {
    margin-bottom: clamp(24px, 3vw, 36px);
  }

  .marquee-wordmarks {
    --wm-gap: 64px;

    overflow: hidden;
    padding: 8px 0;
    -webkit-mask-image: linear-gradient(90deg, transparent 0, black 8%, black 92%, transparent 100%);
    mask-image: linear-gradient(90deg, transparent 0, black 8%, black 92%, transparent 100%);
  }

  .marquee-wordmarks__track {
    display: flex;
    width: max-content;
    animation-name: marquee;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
  }

  .marquee-wordmarks:hover .marquee-wordmarks__track,
  .marquee-wordmarks:focus-within .marquee-wordmarks__track {
    animation-play-state: paused;
  }

  .marquee-wordmarks__group {
    display: flex;
    flex-shrink: 0;
    align-items: center;
    gap: var(--wm-gap);
    padding: 0 var(--wm-gap) 0 0;
    margin: 0;
    list-style: none;
  }

  /* Referans: marka isimleri 26px ve çok silik (filigran hissi). */
  .marquee-wordmarks__group li {
    font: 700 26px var(--font-display);
    color: var(--text-faint);
    white-space: nowrap;
    transition: color var(--t-slow);
  }

  .marquee-wordmarks__group li:hover {
    color: var(--text);
  }

  :global(.section--light) .marquee-wordmarks__group li {
    color: rgba(10, 10, 10, 0.3);
  }

  :global(.section--light) .marquee-wordmarks__group li:hover {
    color: var(--text-on-light);
  }

  @media (prefers-reduced-motion: reduce) {
    .marquee-strip,
    .marquee-wordmarks {
      overflow-x: auto;
      overflow-y: hidden;
    }

    .marquee-strip__track,
    .marquee-wordmarks__track {
      animation: none;
    }
  }
</style>
